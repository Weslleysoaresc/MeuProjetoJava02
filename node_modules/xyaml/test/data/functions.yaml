name: Denis Ivanov
age: 29
nextAge: ${age + 1}
welcome: Hello
getHello: (name) => {
  return `${welcome}, ${name}!`;
};

math:

  base: 2

  methods:
    add: (a, b) => {
      return a + b + parent.base;
    };

  numbers:
    - ${methods.add(1, 2)}
    - ${methods.add(1, 3)}
    - ${methods.add(1, 4)}
    - ${methods.add(1, 5)}
    - ${methods.add(1, 6)}

  sum: ${methods.add(55, 44)}
